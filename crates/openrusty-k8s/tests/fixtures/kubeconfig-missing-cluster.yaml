# Fixture: broken - context references a cluster that does not exist.
current-context: ctx-dev
clusters: []
contexts:
  - name: ctx-dev
    context:
      cluster: no-such-cluster
      user: user-dev
users:
  - name: user-dev
    user:
      token: dummy-token-for-tests
