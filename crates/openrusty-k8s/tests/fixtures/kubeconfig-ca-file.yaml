# Fixture: certificate-authority referenced by file path, relative to the
# kubeconfig location (kubectl semantics under test here).
current-context: ctx-dev
clusters:
  - name: cluster-dev
    cluster:
      server: https://apiserver.example.invalid:6443
      certificate-authority: test-ca.crt
contexts:
  - name: ctx-dev
    context:
      cluster: cluster-dev
      user: user-dev
users:
  - name: user-dev
    user:
      token: dummy-token-for-tests
