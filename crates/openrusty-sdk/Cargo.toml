[package]
name = "openrusty-sdk"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
openrusty-macros = { workspace = true }

[dev-dependencies]
# Compile-fail tests for `#[phase]` signature validation (tests/phase.rs).
trybuild = "1.0"
